{% extends '@App/base.html.twig' %}

{% from '@App/macro/fields.html.twig' import baseField %}

{% block content %}

    <div class="row">
        <div class="col-lg-12">
            <div class="tabs-container">
                <div class="tab-content">
                    <div class="tab-pane active">
                        <div class="panel-body">
                            <fieldset>
                                {{ baseField('Id', project.id) }}
                                {{ baseField('Pildymo data', project.createdAt|date('Y-m-d')) }}
                            </fieldset>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}